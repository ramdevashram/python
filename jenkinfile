pipeline {
    agent any

    stages {
        stage('git') {
            steps {
                git branch: 'main', url: 'https://github.com/ramdevashram/python.git'
            }
        }
        stage('python'){
            steps {
                sh 'python hello.py'
            }
        }
    }
}
